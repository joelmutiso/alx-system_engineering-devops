# 0x03-shell_variables_expansions

## 0-alias
This script creates an alias `ls` that runs `rm *`.  
When sourced using `source ./0-alias`, the alias overrides the `ls` command in the current shell session.  
Use `\ls` to run the original `ls` command without the alias.
